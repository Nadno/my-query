# Memória — mini-q

- [Atributos ARIA como objeto na engine](aria-object-engine.md) — `aria` deveria aceitar objetos reativos como `data` e `on`; requer mudança de engine.
- [Estilos para elementos teleportados](teleported-styles-engine.md) — propostas `>name` outer e `style.scope()` com @scope CSS para elementos que moram fora do pai.
- [`$on`/`$off` mais flexíveis](on-off-engine.md) — `$on` deveria aceitar `Element` além de `MQ`, e deveria existir `$off` para remoção manual.
